{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the session log window",
  "windows": ["main", "log"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "opener:default"
  ]
}

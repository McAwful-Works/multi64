{
  "name": "multi64",
  "private": true,
  "version": "0.1.0",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:standalone": "tauri build --config src-tauri/tauri.standalone.conf.json -- --no-default-features && node rename-standalone.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}

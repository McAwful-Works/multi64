[package]
name = "ap64-core"
description = "Detect an Archipelago N64 seed and splice the M64P cart agent into it"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
sha1 = "0.11"
toml = "0.8"

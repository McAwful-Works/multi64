[package]
name = "sc64-sd-e2e"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "SummerCart64 SD/FAT end-to-end hardware test (list, mkdir, upload, download, rename, delete)"

[[bin]]
name = "sc64-sd-e2e"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
multi64-sc64-sd = { path = "../multi64-sc64-sd" }

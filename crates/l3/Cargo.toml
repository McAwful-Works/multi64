[package]
name = "multi64-l3"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "multi64 L3 bridge protocol (framing, handshake, optional session layer)"

[dependencies]

[dev-dependencies]
